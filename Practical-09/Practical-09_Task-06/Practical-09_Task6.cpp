#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int num=126899;
    cout<<"This is the first line."<<endl;           // 'endl' goes to next line
    cout<<setw(20)<<setfill('*')<<num<<endl;              // 'setw' makes spaces and 'setfill' fills those spaces with the specified character
}